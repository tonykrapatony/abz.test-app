import React from 'react'
import Button from '../../UI/Button'

export default function Buttons() {
  return (
    <div className="buttons">
        <Button className="btn primary-btn">Users</Button>
        <Button className="btn primary-btn">Sign up</Button>
    </div>
  )
}
